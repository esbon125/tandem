# Generated Libero project (regenerate with: libero SCRIPT:build_mpeg2fpga_soc.tcl)
MPEG2FPGA_SOC/
# Regenerated by pfsoc_mss -GENERATE on every build run
script_support/components/MSS/
# Build outputs (bitstream job, digest) and downloaded HSS firmware --
# matches the reference design's own .gitignore convention for these
*.job
*.digest
*.hex
# Ad-hoc fallback-bitstream/build-log backups made around a given change --
# same reasoning as the job/digest ignores above, just a whole directory of
# them; see docs/bringup (docs branch) for the writeup, not these logs.
backups/
